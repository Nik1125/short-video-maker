# 🎯 Как запустить локальную версию Short Video Maker

## 📖 Простыми словами

**Официальная версия** использует готовый Docker образ: `gyoridavid/short-video-maker:latest-cuda`

**Ваша версия** собирает Docker образ **из вашего локального кода** с добавленными функциями.

## 🔄 Разница в docker-compose.yml

### Было (официальная версия):
```yaml
short-video-maker:
  image: gyoridavid/short-video-maker:latest-cuda  # готовый образ
```

### Стало (ваша версия):
```yaml
short-video-maker:
  build:
    context: ..               # собирай из локального кода
    dockerfile: main-cuda.Dockerfile
```

## ⚡ Что происходит при сборке

```
docker compose up --build
     ↓
1. Читает context: .. (ваш проект)
     ↓
2. Копирует src/, static/, package.json и т.д.
     ↓
3. Компилирует TypeScript → JavaScript
     ↓
4. Устанавливает зависимости (kokoro-js, remotion, etc)
     ↓
5. Скачивает Whisper.cpp и компилирует с CUDA
     ↓
6. Собирает финальный Docker образ
     ↓
7. Запускает контейнер с вашим кодом ✨
```

## 🚀 Запуск

```powershell
# 1. Перейти в example
cd example

# 2. Запустить сборку (первый раз ~15 минут)
docker compose up --build

# 3. В следующий раз быстрее (если не меняли код)
docker compose up
```

## ✅ Проверка

После запуска проверьте:

```powershell
# Контейнер работает?
docker ps

# Health
curl http://localhost:3123/health

# Новые фичи работают?
curl http://localhost:3123/api/voices-with-metadata
curl http://localhost:3123/api/custom-audio/
```

## 🔥 Почему локальная сборка?

✅ Ваш код с улучшениями  
✅ Кастомное аудио работает  
✅ Метаданные голосов доступны  
✅ Контроль над кодом  
✅ Можете дальше развивать проект  

## 📚 Полная инструкция

См. [example/INSTALLATION.md](./example/INSTALLATION.md) для деталей.

