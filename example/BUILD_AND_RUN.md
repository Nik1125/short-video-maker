# Сборка и запуск локальной версии

## 📋 Принцип работы

Вместо использования готового образа `gyoridavid/short-video-maker:latest-cuda`, ваш `docker-compose.yml` **собирает образ из вашего локального кода**.

## 🔧 Что делает docker-compose.yml

```yaml
short-video-maker:
  build:
    context: ..              # Берет исходники из родительской папки
    dockerfile: main-cuda.Dockerfile  # Собирает CUDA версию
```

## 🚀 Запуск

### Из папки `example/`:

```powershell
docker compose up --build
```

**Процесс:**
1. Docker читает `context: ..`
2. Копирует все файлы проекта
3. Собирает образ по `main-cuda.Dockerfile`
4. Компилирует TypeScript
5. Скачивает зависимости
6. Устанавливает Whisper.cpp с CUDA
7. Запускает контейнер

### Первая сборка

⏱️ **10-15 минут** из-за:
- Сборки Whisper.cpp с CUDA поддержкой
- Компиляции TypeScript → JavaScript
- Установки всех npm зависимостей
- Загрузки моделей TTS

### Последующие запуски

```powershell
# Быстрый перезапуск без пересборки
docker compose up

# Если изменили код - пересоберите
docker compose up --build
```

## 🎯 Проверка что работает

```powershell
# 1. Контейнер запущен
docker ps | findstr short-video-maker

# 2. Health check
curl http://localhost:3123/health

# 3. Аудио доступно
curl http://localhost:3123/api/custom-audio/

# 4. Логи
docker logs short-video-maker -f
```

## 🔄 Обновление после изменения кода

```powershell
# Остановите контейнеры
docker compose down

# Пересоберите образ
docker compose up --build

# Или принудительная пересборка без кэша
docker compose build --no-cache
docker compose up
```

## 💡 Альтернативы Dockerfile

Если нет GPU, измените в `docker-compose.yml`:

```yaml
# Вместо:
dockerfile: main-cuda.Dockerfile

# Используйте:
dockerfile: main-tiny.Dockerfile    # для слабого железа
# или
dockerfile: main.Dockerfile         # стандартная версия
```

## 🐛 Troubleshooting

### Ошибка: "Cannot find module"

**Решение:** Пересоберите образ:
```powershell
docker compose down
docker compose up --build
```

### Ошибка: CUDA not available

**Проверьте:**
```powershell
nvidia-smi  # должна показать GPU
```

**Или** используйте `main-tiny.Dockerfile` без CUDA.

### Контейнер падает при старте

**Смотрите логи:**
```powershell
docker logs short-video-maker
```

**Частые причины:**
- Нет PEXELS_API_KEY
- Недостаточно RAM (< 3GB)
- Неправильный путь в volumes

## ✅ Готово!

После успешного запуска ваш Short Video Maker с кастомным аудио готов к работе!

